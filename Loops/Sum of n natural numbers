#include<stdio.h>
#include<math.h>
int main(){
 int n;
  int sum=0;
  printf("enter the value of n:");
  scanf("%d",&n);
  for(int i=1; i<=n; i++){   //putting n=i puts you into an infinite loop
      sum+=i;
      printf("sum is %d\n", sum);
  }
  return 0;
  }
